[project]
name = "taxi-mlops-starter"
version = "0.1.0"
description = "Минимальный стартовый проект MLOps для прогнозирования стоимости проезда в такси."
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "scikit-learn",
    "joblib",
    "fastapi",
    "uvicorn",
    "mlflow"
]