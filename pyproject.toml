[project]
name = "taxi-mlops-starter"
version = "0.1.0"
description = "Minimal MLOps starter project for taxi fare prediction."
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "scikit-learn",
    "joblib",
    "fastapi",
    "uvicorn",
    "mlflow"
]
